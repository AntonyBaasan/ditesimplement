version: '2'
services:
    ditesimplement-mongodb:
        image: mongo:3.4.10
        ports:
            - "27017:27017"
        command: mongos --configdb ditesimplement-mongodb-config
    ditesimplement-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    ditesimplement-mongodb-config:
        image: mongo:3.4.10
        container_name: ditesimplement-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
